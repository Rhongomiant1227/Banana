# 🍌 Nano Banana - AI 图像生成工具

基于智增增平台的 AI 图像生成应用，简单易用，支持剪贴板粘贴。

![Nano Banana](https://img.shields.io/badge/Nano%20Banana-AI%20Image-yellow)
![Python](https://img.shields.io/badge/Python-3.8+-blue)
![Node.js](https://img.shields.io/badge/Node.js-18+-green)

## ✨ 功能特点

- 🎨 **AI 图像生成** - 通过文字描述生成高质量图像
- 📋 **剪贴板粘贴** - 支持 Ctrl+V 直接粘贴参考图
- 🖱️ **拖拽上传** - 拖拽图片到上传区域
- 📐 **灵活比例** - 支持多种宽高比，或让模型自动决定
- 🍌 **双模型支持**:
  - **Nano Banana Pro** (Gemini 3 Pro) - 专业版，支持最高 4K 分辨率
  - **Nano Banana** (Gemini 2.5 Flash) - 快速版，高效生成
- 📜 **历史记录** - 自动保存生成历史
- 🎭 **演示模式** - 无需 API 密钥即可体验

## 🚀 快速开始

### 环境要求

- Python 3.8+
- Node.js 18+
- npm 或 yarn

### 一键启动

**Windows:**
```bash
双击运行 start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

### 手动启动

1. **安装后端依赖**
```bash
cd backend
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

pip install -r requirements.txt
```

2. **配置 API 密钥**
```bash
cp .env.example .env
# 编辑 .env 文件，填入你的 API 密钥
```

3. **启动后端**
```bash
python -m uvicorn app:app --reload --port 8000
```

4. **安装前端依赖**
```bash
cd frontend
npm install
```

5. **启动前端**
```bash
npm run dev
```

6. **访问应用**
打开浏览器访问 http://localhost:3000

## 📖 使用说明

### 基本使用

1. 在提示词框中输入你想要生成的图像描述
2. （可选）上传参考图片或使用 Ctrl+V 粘贴
3. 选择宽高比和模型
4. 点击"生成图像"按钮
5. 等待生成完成后下载图片

### 演示模式

默认开启演示模式，使用模拟数据，无需配置 API 密钥即可体验完整功能。

关闭演示模式后，需要在 `backend/.env` 中配置真实的 API 密钥。

### API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/generate` | POST | 提交生成任务 |
| `/api/task/{id}` | GET | 查询任务状态 |
| `/api/tasks` | GET | 获取任务列表 |
| `/api/set-key` | POST | 设置 API 密钥 |
| `/api/check-key` | GET | 检查密钥状态 |
| `/api/verify-key` | POST | 验证密钥有效性 |
| `/api/demo/generate` | POST | 演示模式生成 |
| `/api/upload-reference` | POST | 上传参考图 |

## 🔧 配置说明

### 环境变量 (backend/.env)

```env
# 智增增平台 API 密钥
NANOBANANA_API_KEY=your_api_key_here
```

> 💡 API 密钥也可以通过网页界面设置，设置后会自动保存。

### 支持的模型

| 前端名称 | 真实模型名 | 说明 |
|---------|-----------|------|
| Nano Banana Pro | `gemini-3-pro-image-preview` | Gemini 3 Pro，专业版，支持 4K |
| Nano Banana | `gemini-2.5-flash-image` | Gemini 2.5 Flash，快速版 |

### 获取 API 密钥

1. 访问 [智增增平台](https://zhizengzeng.com)
2. 注册/登录账号
3. 在控制台获取 API 密钥

## 📁 项目结构

```
banana/
├── backend/              # Python 后端
│   ├── app.py           # FastAPI 主应用
│   ├── requirements.txt # Python 依赖
│   ├── .env.example     # 环境变量示例
│   └── .env             # 环境变量（需创建）
├── frontend/             # React 前端
│   ├── src/
│   │   ├── App.jsx      # 主组件
│   │   ├── main.jsx     # 入口文件
│   │   └── index.css    # 样式文件
│   ├── package.json     # Node 依赖
│   └── vite.config.js   # Vite 配置
├── start.bat            # Windows 启动脚本
├── start.sh             # Linux/Mac 启动脚本
└── README.md            # 说明文档
```

## 🛠️ 技术栈

### 后端
- **FastAPI** - 高性能 Python Web 框架
- **uvicorn** - ASGI 服务器
- **httpx** - 异步 HTTP 客户端
- **pydantic** - 数据验证

### 前端
- **React 18** - UI 框架
- **Vite** - 构建工具
- **TailwindCSS** - CSS 框架
- **Lucide React** - 图标库

## 📝 更新日志

### v1.0.0
- 初始版本发布
- 支持文生图功能
- 支持剪贴板粘贴
- 支持拖拽上传
- 演示模式

## 📄 许可证

MIT License

---

Made with ❤️ by 智增增平台
